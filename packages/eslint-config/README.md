# @club-os/eslint-config

Placeholder package for shared linting configuration.

Claude Code implementation step:
- Add eslint flat config.
- Add framework-specific overrides for Next.js and React Native.
- Add import boundary rules to enforce module architecture.
